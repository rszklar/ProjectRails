<p id="notice"><%= notice %></p>
<br /><br /><br /><br />
<section class="success" id="about">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 text-center">
                    <h2><%= @ad.title %></h2>
                    <hr class="star-light">
                </div>
            </div>
            <div class="row">
                <div class="col-lg-5 col-lg-offset-2">
                    <p><%= @ad.desc %></p>
					
                </div>
                <div class="col-lg-3">
                    <p>Autor: <%= @ad.name %></p>
					<p>Telefon: <%= @ad.tel %></p>
                </div>
                <div class="col-lg-8 col-lg-offset-2 text-center">
                    <h3><mark>
                        <%= link_to 'Back', ads_path %>
						<% if session[:user_id] %> |
						<%= link_to 'Edytuj', edit_ad_path(@ad) %></mark></h3> 
						<% end %><br />
						<% if session[:user_id] %>
						<%= button_to 'Usuń', @ad, method: :delete, data: { confirm: 'Are you sure?' } %>
						<% end %>
                    
                </div>
            </div>
        </div>
    </section>


